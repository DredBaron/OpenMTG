[pytest]
# Run from the repo root:  pytest backend/tests/
# Or from within the backend directory:  pytest tests/
testpaths = backend/tests
pythonpath = backend
addopts = -v --tb=short
filterwarnings =
    ignore::DeprecationWarning

[pytest:env]
DATABASE_URL=sqlite://
